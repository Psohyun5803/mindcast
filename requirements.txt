# ================================================================
# mindcast_pipeline 통합 requirements
# suicide_predict (일별/월별 예측기) + mindcast_online (이벤트 트래커)
# ================================================================

# ── 딥러닝 ───────────────────────────────────────────────────────
# torch는 CUDA 버전에 맞춰 별도 설치 권장:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
torch>=2.0.0
torchvision
torchaudio

# ── HuggingFace ──────────────────────────────────────────────────
transformers>=4.40.0
datasets>=2.0.0
huggingface_hub>=0.20.0
tokenizers
sentence-transformers

# ── 데이터 처리 ───────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
pyarrow
scikit-learn>=1.3.0
scipy>=1.10.0
fsspec

# ── 시각화 ────────────────────────────────────────────────────────
matplotlib>=3.7.0

# ── 확률 예측 (suicide_predict) ───────────────────────────────────
properscoring

# ── 한국 공휴일 캘린더 (suicide_predict) ──────────────────────────
holidays

# ── 한국어 NLP (mindcast_online prep) ────────────────────────────
kiwipiepy

# ── Excel I/O (emotion_classifier) ───────────────────────────────
openpyxl

# ── 기타 ──────────────────────────────────────────────────────────
tqdm
joblib
requests
